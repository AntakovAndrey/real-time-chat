<UserControl x:Class="ChatClientWpf.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Авторизация" FontSize="24" Margin="0,0,0,20" HorizontalAlignment="Center"/>
            <Label>Имя пользователя</Label>
            <TextBox Text="{Binding Username}" Margin="0,0,0,10" Padding="5"/>
            <Label>Пароль</Label>
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,0,0,10" Padding="5" />
            <Button Content="Войти" Command="{Binding LoginCommand}" Margin="0,0,0,10" Padding="10,5"/>
            <TextBlock HorizontalAlignment="Center">
                <Hyperlink Command="{Binding NavigateToRegisterCommand}">
                    Нет аккаунта? Зарегистрироваться
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>