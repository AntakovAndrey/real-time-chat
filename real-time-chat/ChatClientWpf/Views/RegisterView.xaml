<UserControl x:Class="ChatClientWpf.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Регистрация" FontSize="24" Margin="0,0,0,20" HorizontalAlignment="Center"/>
            <Label>Имя</Label>
            <TextBox Text="{Binding Name}" Margin="0,0,0,10" Padding="5" />
            <Label>Фамилия</Label>
            <TextBox Text="{Binding Surname}" Margin="0,0,0,10" Padding="5"/>
            <Label>Имя пользователя</Label>
            <TextBox Text="{Binding Username}" Margin="0,0,0,10" Padding="5" />
            <Label>Email</Label>
            <TextBox Text="{Binding Email}" Margin="0,0,0,10" Padding="5"/>
            
            <Label>Пароль</Label>
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_OnPasswordChanged" Margin="0,0,0,10" Padding="5"/>
            
            <Label>Подтвердите пароль</Label>
            <PasswordBox x:Name="ConfirmPasswordBox" PasswordChanged="ConfirmPasswordBox_OnPasswordChanged" Margin="0,0,0,10" Padding="5"/>
            
            <Button Content="Зарегистрироваться" Command="{Binding RegisterCommand}" 
                    Margin="0,0,0,10" Padding="10,5"/>
            
            <TextBlock HorizontalAlignment="Center">
                <Hyperlink Command="{Binding NavigateToLoginCommand}">
                    Уже есть аккаунт? Войти
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>