<UserControl x:Class="ChatClientWpf.Views.SearchResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#F5F5F5">
            <Button Content="Все" 
                    Command="{Binding SetQuickSearchCommand}"
                    Style="{Binding CurrentSearchType, Converter={StaticResource SearchTypeToButtonStyleConverter}, ConverterParameter=Quick}"/>
            
            <Button Content="Сообщения" 
                    Command="{Binding SetMessagesSearchCommand}"
                    Style="{Binding CurrentSearchType, Converter={StaticResource SearchTypeToButtonStyleConverter}, ConverterParameter=Messages}"/>
            
            <Button Content="Пользователи" 
                    Command="{Binding SetUsersSearchCommand}"
                    Style="{Binding CurrentSearchType, Converter={StaticResource SearchTypeToButtonStyleConverter}, ConverterParameter=Users}"/>
            
            <Button Content="Чаты" 
                    Command="{Binding SetChatsSearchCommand}"
                    Style="{Binding CurrentSearchType, Converter={StaticResource SearchTypeToButtonStyleConverter}, ConverterParameter=Chats}"/>
        </StackPanel>
        
        <Border Grid.Row="1" Background="#CCFFFFFF" 
                Visibility="{Binding IsSearching, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="100" Height="10"/>
                <TextBlock Text="Поиск..." Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <ListBox Grid.Row="1" ItemsSource="{Binding SearchResults}" 
                 >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Avatar}" FontSize="20" Margin="0,0,10,0"
                                   VerticalAlignment="Center" Grid.Column="0"/>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding Description}" Foreground="Gray" TextTrimming="CharacterEllipsis"
                                       FontSize="12" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>