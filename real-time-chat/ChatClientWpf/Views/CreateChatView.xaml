<UserControl x:Class="ChatClientWpf.Views.CreateChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ChatClientWpf.Views"
             xmlns:helpers="clr-namespace:ChatClientWpf.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#E92AB4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E92AB4"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#8B0967"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <helpers:ImagePathToBitmapConverter x:Key="ImagePathToBitmapConverter"/>
        <helpers:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Border Padding="20">
        <StackPanel>
            <TextBlock Text="Новый чат" FontSize="24" FontWeight="Bold" Margin="0,0,0,30"/>
            
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                <Border Width="120" Height="120" Background="#F8F9FA" 
                        CornerRadius="60" BorderThickness="2" BorderBrush="#E9ECEF"
                        Margin="0,0,0,15">
                    <Grid>
                        <Ellipse Width="116" Height="116">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="{Binding AvatarPath, Converter={StaticResource ImagePathToBitmapConverter}}"
                                             Stretch="UniformToFill"/>
                            </Ellipse.Fill>
                        </Ellipse>
                        
                        <Ellipse Width="116" Height="116" Fill="#E9ECEF"
                                 Visibility="{Binding AvatarPath, Converter={StaticResource NullToVisibilityConverter}}"/>
                        
                        <Image Source="/Resources/img/chat.png" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center" 
                               Width="70" 
                               Height="70"
                               Visibility="{Binding AvatarPath, Converter={StaticResource NullToVisibilityConverter}}"
                               />
                        
                        <Border Background="#80000000" CornerRadius="60" 
                                Opacity="0" x:Name="AvatarOverlay">
                            <Border.Triggers>
                                <EventTrigger RoutedEvent="MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation To="1" Duration="0:0:0.2" 
                                                             Storyboard.TargetProperty="Opacity"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation To="0" Duration="0:0:0.2" 
                                                             Storyboard.TargetProperty="Opacity"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Border.Triggers>
                            
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="📷" FontSize="20" Foreground="White" 
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="Изменить" FontSize="11" Foreground="White" 
                                           HorizontalAlignment="Center" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Загрузить фото" Command="{Binding UploadAvatarCommand}"
                            Style="{StaticResource ModernButton}" Padding="12,6"
                            Margin="0,0,8,0"/>
                </StackPanel>
                
                <TextBlock Text="{Binding AvatarInfo}" FontSize="11" Foreground="Gray" 
                           HorizontalAlignment="Center" Margin="0,8,0,0"
                           TextWrapping="Wrap" MaxWidth="200"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Название" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <TextBox Text="{Binding ChatName}" Padding="12,8" Margin="0,0,0,10"
                         FontSize="14" BorderThickness="1" BorderBrush="#D1D5DB"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Создать чат" Command="{Binding CreateChatCommand}" 
                        Style="{StaticResource ModernButton}" Padding="20,10"
                        Margin="0,0,10,0" Background="#E92AB4" Foreground="White"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
